# FinPie Requirements
# 
# IMPORTANT: For GPU acceleration with CUDA support, install PyTorch first:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
# 
# Then install remaining requirements:
# pip install -r requirements.txt
#
# For CPU-only installation, use:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu

numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
matplotlib>=3.7.0
seaborn>=0.12.0
yfinance>=0.2.0
pandas-datareader>=0.10.0
statsmodels>=0.14.0
scikit-learn>=1.2.0
pytest>=7.3.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0
python-dotenv>=1.0.0
requests>=2.28.0
tqdm>=4.65.0
jupyter>=1.0.0
notebook>=6.0.0
ipykernel>=6.0.0
alpha_vantage>=2.3.1

# Deep Learning Libraries - CUDA Support
# Install PyTorch with CUDA support using:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
torch>=2.6.0
torchvision>=0.21.0
torchaudio>=2.6.0
transformers>=4.36.0
datasets>=2.16.0
peft>=0.7.0
bitsandbytes>=0.41.0

# Optional dependencies
# MetaTrader5 is only available on Windows
MetaTrader5>=5.0.4993; platform_system=="Windows"

ta-lib>=0.6.7

# Alternative data sources (optional)
# Uncomment and install as needed based on your API subscriptions
# twelve-data>=1.2.0
# polygon-api-client>=1.8.0
# iexfinance>=0.5.0 